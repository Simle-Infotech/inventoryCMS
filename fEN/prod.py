from .settings import *

DEBUG = False
ALLOWED_HOSTS = ['family.simle.com.np']

# STATIC_ROOT = os.path.join(os.path.dirname(BASE_DIR), 'public', 'fEN', 'static')

# MEDIA_ROOT = os.path.join(os.path.dirname(BASE_DIR), 'public', 'fEN', 'media')
